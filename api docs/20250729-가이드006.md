# Casino-Club F2P 최종 체크리스트 — 요약본 (2025-08-13)

이 문서는 `20250729-가이드006.md`의 핵심만 남긴 요약판입니다. 세부 설명/예시는 Git history에서 원문을 확인하세요.

## 0) 운영 원칙 & 성공 기준
- 컨테이너 기준: docker-compose, 루트 `docker-manage.ps1`로 실행/테스트/마이그레이션 관리
- Alembic: 단일 head 유지(현재 기준: f79d04ea1016), 수정 시 머지 전략 필수
- 설정 import 표준: `from app.core.config import settings` (config.py 확장 금지)
- 성공 기준: 빌드/테스트 그린, alembic heads 단일, `/health` 200, `/docs` 정상, 중복 파일/스키마 없음

## 1) 작업 루프(필수)
1. 서비스 상태: `./docker-manage.ps1 status`
2. 마이그레이션 체인: 컨테이너에서 `alembic heads` (단일 head 확인)
3. 테스트: 컨테이너에서 `pytest -q app/tests`
4. 스모크: `/docs` 확인 → 보호 API 401 → 로그인 → 200
5. 필요 시 OpenAPI 재수출: 컨테이너에서 `python -m app.export_openapi`

## 2) 핵심 API 현황(요약)
- 인증: `/api/auth/signup`, `/api/auth/login`, `/api/auth/refresh`, `/api/auth/me`
- 유저: `/api/users/profile`, `/api/users/{user_id}`
- 보상: `POST /api/rewards/distribute`(명세에 따라 `/api/rewards`로 표기된 곳도 있음), `GET /api/rewards/users/{user_id}/rewards`
- 게임: 슬롯/가챠 스핀 등(세부는 OpenAPI/코드 주석 확인)

## 3) 보상 지급 API 체크리스트(필수 유지)
- [ ] 보상 지급 API (POST /api/rewards)
  - 체크 방법: 다양한 보상 유형 지급 테스트, 동시 다수 요청 처리 테스트
  - 검증 기준: 사용자 잔액/인벤토리 정확히 업데이트, 중복 지급 방지
  - 체크 대상:
    - [✅] 보상 유형별 처리 (코인, 젬, 아이템) — TOKEN/COIN 통화 반영, ITEM 기록
    - [✅] 트랜잭션 처리 (원자성) — Reward + UserReward 동시 커밋, 오류 시 롤백
    - [✅] 중복 지급 방지 (idempotency) — UserAction(REWARD_GRANT + idempotency_key)로 재시도 안전
    - [✅] 보상 히스토리 기록 — GET /api/rewards/users/{user_id}/rewards 페이지네이션, claimed_at 기준 정렬
    - [⚠️] 특별 보상 처리 (이벤트, 프로모션) — 범위 확장 시 추가 모델링 예정
    - [✅] 알림 연동 (보상 지급 알림) — WebSocket best-effort 개인 알림 전송

  검증 메모(2025-08-12):
  - `app/tests/test_rewards_distribution.py` 2건 PASS. 목록 API는 UserReward↔Reward 조인으로 RewardItem(reward_id, reward_type, reward_value, awarded_at=claimed_at) 형태 반환하도록 수정.

## 4) 데이터/마이그레이션/Kafka — 최소 운용 가이드
- Alembic
  - heads/history 점검: 컨테이너에서 `alembic heads`, `alembic history --verbose`
  - 단일 head 유지, 머지 리비전 활용. SQLite 개발환경은 batch_op 우선(운영 PG에는 미적용)
- DB 분리(테스트)
  - `.env.test`로 테스트 DB 격리, `docker-compose.test.yml` 병합 기동
  - 테스트 루틴: `alembic upgrade head` → `pytest`
- Kafka(선택)
  - 설정: `KAFKA_ENABLED`, `KAFKA_BOOTSTRAP_SERVERS`
  - 엔드포인트: `/api/kafka/health`, `/api/kafka/produce` (브로커 없으면 health=disabled, 테스트 skip)

## 5) 금지/주의(반드시 준수)
- 라우터/서비스/스키마/마이그레이션/문서 중복 생성 금지
- Alembic revision ID 중복 금지(다중 head 발생 시 merge로 통합)
- 파일명 접미사 `_simple/_fixed/_temp/_v2` 금지
- 환경 변수/설정 키 변경 시 문서와 dotenv/compose 동기화

## 6) 다음 단계(팀 공통)
- 보상 지급 API: 동시성/멱등 재검증(부하 100rps 샘플), WebSocket 알림 체인 스모크
- Alembic: 변경 시 단일 head 보장 및 OpenAPI 재수출 동반
- 테스트: `app/tests` 커버리지 보강(핵심 80%+ 목표), 실패 시 즉시 원인/대응 기록

> 원문 전체는 Git history에서 확인 가능합니다. 본 요약본은 운영/개발 실무에 필요한 최소 정보만 유지합니다.

## 7) 마스터 체크리스트 — E2E/풀스택/배포 전 최종

### 7.1 전체 유저 플로우(엔드투엔드)
- [ ] 컨테이너 기동: `./docker-manage.ps1 start --tools` → `status` 200/OK 확인(backend /health, frontend 3000)
- [ ] 회원가입/로그인: `POST /api/auth/signup` → `POST /api/auth/login` → `GET /api/users/profile` 200
- [ ] 토큰 리프레시: `POST /api/auth/refresh`(헤더/바디 경로 모두) → 새로운 access_token 발급 확인(jti/iat 변경)
- [ ] 게임 플레이: 슬롯/가챠/크래시 최소 1회 호출 → 성공 응답 및 사용자 잔액/히스토리 증가
- [ ] 보상 수령: `POST /api/rewards/distribute`(또는 도메인 액션 후 자동) → `GET /api/rewards/users/{id}/rewards`에서 기록 확인(정렬: claimed_at desc)
- [ ] 미션/이벤트: 참여 → 진행 → 수령(`POST /api/events/claim/{event_id}`, `POST /api/missions/claim/{mission_id}`) 성공
- [ ] 상점/결제(프리미엄 재화): 더미 결제/보상 로직 스모크(가능 시) → 통화/인벤토리 업데이트 확인
- [ ] WebSocket 알림: `/ws/notifications/{user_id}` 연결 후 보상/관리자 브로드캐스트 수신 확인

결과 기준
- [ ] API 2xx, 오류는 4xx로 수렴(5xx 없음)
- [ ] DB(User/Rewards/Actions 등) 반영 정상, Redis(세션/임시 상태) 키 생성·정리 정상
- [ ] OpenAPI `/docs` 경로/스키마와 실제 동작 일치

### 7.2 백엔드 API/도메인 검증
- 인증/유저
  - [ ] `/api/auth/*` 4종(signup/login/refresh/me) 동작, 락아웃/레이트리밋 정책 문서 일치
  - [ ] `/api/users/profile`, `/api/users/{user_id}` 권한 모델 일치(자기 정보/타인 제한)
- 게임
  - [ ] `/api/games/gacha/pull`, `/api/games/slot/spin`(있다면), `/api/games/crash/*` 정상
  - [ ] RNG/분포 테스트 존재 및 통과(허용오차 문서 일치)
- 보상
  - [ ] `POST /api/rewards/distribute` 원자성, 멱등키(idempotency_key) 중복 방지, WebSocket 알림
  - [ ] `GET /api/rewards/users/{id}/rewards` 페이지네이션/정렬/카운트 일치
- 이벤트/미션
  - [ ] 참여/진행/수령 경로 정상, 재수령 방지(claimed 플래그) 동작
- 관리자(Admin)
  - [ ] `POST /api/admin/rewards/grant-bulk` 결과 배열 ok/error 포함
  - [ ] (선택) `/api/admin/notifications/*` 브로드캐스트/타깃 발송 검증

### 7.3 프론트엔드 연동 검증
- [ ] `.env` 설정(NEXT_PUBLIC_API_URL 등) 일치, 401→refresh→재시도 인터셉터 동작
- [ ] 주요 화면(프로필/보상함/게임/상점) API 계약 일치, 타입 정의 최신화(openapi 기반 권장)
- [ ] WebSocket 클라이언트 자동 재연결/heartbeat/토큰 갱신 반영
- [ ] 접근성/반응형/성능(공통 레이아웃, 애니메이션 과부하 점검)

### 7.4 데이터/마이그레이션/시드
- [ ] Alembic `alembic heads` 단일 head(현재: f79d04ea1016)
- [ ] `alembic upgrade head` 성공, 충돌/다중 head 미발생(필요 시 merge 리비전 마련)
- [ ] 시드 스크립트(게임/기본 데이터) 적용 확인, 테스트 DB 격리(.env.test, compose.test)

### 7.5 성능/부하/동시성(최소 기준)
- [ ] 보상 지급 100rps 60초 부하에서 에러율 < 1%, p95 < 300ms(로컬 기준 가이드)
- [ ] 동시 중복 지급 재시도 시 멱등키로 단 1회 반영됨 확인
- [ ] 크래시 세션 단일 활성 가드(중복 베팅 시 409) 확인

### 7.6 보안/RBAC/규정
- [ ] JWT 시크릿/만료/알고리즘 설정 유효, 리프레시 회전/폐기 플로우 테스트 통과
- [ ] RBAC 가드(standard/premium/vip) 경로 검증, CORS/HTTPS(운영) 정책 확인
- [ ] 로그인 실패 잠금/레이트리밋 문서와 코드 일치

### 7.7 관측/로깅/알림
- [ ] 구조화 로그(요청 id, 사용자 id, 경로, 지연) 기록 확인
- [ ] 알림(WS) best-effort 전달, 연결 수명/에러 핸들링 정상
- [ ] (선택) Kafka 사용 시 health/produce/consume 스모크 통과

### 7.8 문서/OpenAPI/테스트
- [ ] 컨테이너 내부 `python -m app.export_openapi`로 스키마 최신화, `/docs` 단일 태그 체계 유지
- [ ] 핵심 pytest 스위트 통과(app/tests, auth_token 픽스처 사용), 경고는 문서화/감소 추세
- [ ] 본 문서 체크박스 최신 유지, 변경 시 `api docs/20250808.md`에 요약/검증/다음 단계 갱신

### 7.9 배포 전 최종 점검(Pre-flight)
- [ ] 환경변수 점검(.env.production, compose.prod): JWT_SECRET_KEY/DB/REDIS/KAFKA/도메인/CORS
- [ ] DB 백업/롤백 플랜 문서화, Alembic downgrade 경로 체크(핵심 리비전)
- [ ] 헬스체크/프로브 구성(backend /health, frontend 응답), 모니터링 대시보드 가용

### 7.10 롤백/DR
- [ ] DB 스냅샷/백업 복구 리허설 문서화
- [ ] Alembic `downgrade -1` 스모크(비파괴 범위 내) 또는 시뮬레이션 검토
- [ ] 기능 토글/플래그로 위험 변경 비활성화 가능

### 7.11 사인오프(역할별)
- [ ] 백엔드 오너 확인: API/DB/마이그레이션/테스트 상태 OK
- [ ] 프론트 오너 확인: 페이지/상태/타입/빌드 OK
- [ ] 데브옵스 오너 확인: 배포/롤백/모니터링 OK
- [ ] PM/QA 확인: 사용자 플로우/E2E 체크리스트 전 항목 OK

## 8) 기능별 세부 체크리스트 (구현 상태/미구현 포함)

설명: 각 항목 끝의 상태 표시
- [✅ 구현] 이미 구현/테스트 통과
- [⏳ 진행] 부분 구현/검증 진행 중
- [TBD] 미구현, 설계/우선순위 필요
 - [⏸ 보류] 차기 패치로 이월(현 릴리스 스코프 아님)

### 8.1 인증/유저
- [✅] 회원가입/로그인/리프레시/로그아웃(-all) 플로우, jti/iat 회전 및 저장소 연동
- [⏳] 로그인 실패 잠금/레이트리밋 정책, 문서/설정과 일치 검증
- [✅] RBAC 가드(standard/premium/vip) 및 데모 라우터 검증
- [✅] 프로필: `/api/users/profile`(me) vs `/api/users/{user_id}` 권한 모델 일치
- [✅] OpenAPI 스키마 최신화 및 프론트 타입 동기화 경로 문서

### 8.2 게임
- [✅] 가챠: `/api/games/gacha/pull` 분포 테스트(±5%) 통과, 시드 고정, near-miss 규칙 문서 반영
- [⏳] 슬롯: `/api/games/slot/spin` 스펙/테스트 보강(있다면)
- [✅] 크래시: bet/cashout, 단일 활성 세션 가드(409), cashout 404, Redis 세션/TTL, profit-only 정산
- [⏳] 게임 히스토리/리포트 API(필요 시)
- [TBD] pity(천장) 파라미터 설정화 및 문서/테스트 연동

### 8.3 보상
- [✅] `POST /api/rewards/distribute` 원자성/멱등키/WS 알림
- [✅] `GET /api/rewards/users/{id}/rewards` 페이지네이션/정렬/카운트 일치
- [✅] Admin 대량지급: `POST /api/admin/rewards/grant-bulk` 결과 배열 ok/error 포함
- [TBD] 보상 템플릿 CRUD, 지급 취소/정정 플로우

### 8.4 상점/결제(Freemium)
- [⏳] 프리미엄 재화(Gems) 구매 API/검증 로직(샌드박스) 스모크
- [TBD] 결제 영수증 검증/재전송/중복 차단, 환불/차감 정책
- [TBD] 한정 패키지/가격 테이블/노출 규칙 및 실험 플래그

### 8.5 배틀패스
- [⏸] 상태 조회/XP 적립/보상 수령(Free 트랙) 기본 플로우
- [⏸] 유료 트랙(삭제: 본 릴리스 스코프 제외)
- [TBD] 시즌 롤오버/리셋, 시즌 보관 정책
- [⏸] Redis XP 카운터/일일 캡(필요 시)

### 8.6 이벤트/미션
- [✅] 참여/진행/수령 사용자 플로우 및 재수령 방지(claimed)
- [TBD] Admin 이벤트/미션 CRUD, 퍼블리시/비활성화, 템플릿
- [⏳] 진행률 산출 규칙(행동/시간 기반) 문서-코드 정합성

### 8.7 개인화/세그먼트
- [⏸] RFM 야간 배치(APScheduler)로 rfm_group/ltv/risk_profile 산출/업데이트
- [⏸] `/recommend/personalized` 추천 엔드포인트, 위험도 반영, AI 메시지 키 연동
- [⏸] 실시간 트리거(예: EARN_CYBER_TOKENS) 시 Redis 즉시 업데이트/푸시

### 8.8 성인 컨텐츠
- [⏸] 연령 인증/토글, Teaser/Partial/Full 단계 잠금 해제 로직
- [⏸] 접근 감사 로그/감사 리포트

## 10) 우선순위 실행 순서 (스코프 조정 반영)

1) 필수 안정화/운영 준비
- [ ] Alembic 단일 head 유지 및 마이그레이션 검증, OpenAPI 재수출
- [ ] 인증/유저 플로우(가입/로그인/리프레시/프로필), RBAC 가드 확정
- [ ] 보상 지급(원자성/멱등/WS), 보상 내역 조회, Admin 대량지급
- [ ] 게임 핵심(가챠/크래시) 스모크/분포/세션 가드
- [ ] 관측/알림 기초(구조화 로그, 기본 대시보드/알림 룰)

2) 제품 기능 마감(스코프 유지)
- [ ] 배틀패스 Free 트랙: XP 적립/수령, UX 마감(유료 트랙 제거 반영)
- [ ] 이벤트/미션 사용자 플로우 정리(수령/재수령 방지), 문서/테스트 보강
- [ ] 프론트 .env/401→refresh 재시도/WS 안정화, 주요 화면 타입/계약 최신화

3) 운영 하드닝/성능
- [ ] compose.prod 리소스 제한/헬스체크 최적화, DB 백업·복구 리허설
- [ ] 부하 목표(p95/에러율) 충족 테스트, 멱등/경합 시나리오 집중 검증
- [ ] 보안 점검(시크릿, TLS/CORS, 의존성 스캔)

4) 보류 항목(차기 패치)
- [ ] 개인화 전반(RFM/추천/실시간 트리거) — ⏸
- [ ] 성인 컨텐츠(연령 인증/잠금 해제/감사) — ⏸
- [ ] 배틀패스 유료 트랙 — 삭제

---

## 11) 실행 순서 — 검증/구현/연동 (실무용)

단계 0. 환경/기초 검증
- [ ] docker-manage.ps1 start --tools → status 확인(back: /health 200, front: 3000)
- [ ] 컨테이너 내부: alembic heads(단일), alembic upgrade head, python -m app.export_openapi
- [ ] pytest -q app/tests 스모크, /docs 로드 OK

단계 1. 인증/유저 안정화(검증→보강)
- [ ] signup/login/refresh/me 스모크(바디/Authorization 양경로)
- [ ] 락아웃/레이트리밋 정책 재확인(문서와 일치), RBAC 가드 테스트
- [ ] 프론트 .env/토큰 저장/401→refresh→재시도 인터셉터 점검

단계 2. 보상/게임 핵심(검증→보강)
- [ ] 보상: distribute 원자성/멱등키/WS, rewards 목록 페이지네이션/정렬
- [ ] 게임: 가챠 분포 테스트(±5%), 크래시 bet→debug→cashout, 중복 가드(409/404)
- [ ] Admin: grant-bulk 결과 배열 ok/error

단계 3. 제품 마감(스코프 반영)
- [ ] 배틀패스 Free 트랙: XP 적립/보상 수령 UX/문서/테스트 마감(유료 트랙 제거 반영)
- [ ] 이벤트/미션: 참여→진행→수령, 재수령 방지/테스트 보강
- [ ] 프론트 주요 화면(프로필/보상함/게임/상점) API 타입/계약 최신화

단계 4. 운영 하드닝/성능
- [ ] compose.prod 리소스 제한/헬스체크, 무중단 배포 전략 점검
- [ ] DB 백업·복구 리허설, Alembic 다운그레이드 시뮬레이션(가능 범위)
- [ ] 부하 테스트(100rps 기준) p95/에러율 목표, 멱등/경합 시나리오 재현
- [ ] 보안 점검(시크릿/Vault, TLS/CORS, 의존성 스캔)

단계 5. 관측/알림/런북
- [ ] 구조화 로그 필드 점검(요청 id/사용자 id/경로/지연)
- [ ] 대시보드 4대 시그널 및 알림 룰(SLO 위반/5xx 급증/라그)
- [ ] 런북/온콜/DR 리허설 일정 등록, 역할별 사인오프 수집

단계 6. 보류/로드맵 관리
- [ ] 개인화 전반(RFM/추천/실시간) — 보류 항목 로드맵 이동
- [ ] 성인 컨텐츠 — 보류 항목 로드맵 이동
- [ ] 배틀패스 유료 트랙 — 삭제로 종결, 회귀 필요 시 별도 RFC

### 8.9 알림 시스템
- [✅] WebSocket: 개인 알림, 관리자 브로드캐스트/타깃 발송
- [TBD] 예약/스케줄 발송(Celery/APScheduler), 이메일/SSE 채널 보강
- [⏳] 재연결/백오프/토큰 갱신 핸들링 프론트 표준화

### 8.10 Admin
- [⏳] 사용자 목록/상세/활동/보상 조회 API
- [⏳] PATCH 편집/승격/ban-unban, 감사로그
- [✅] 공지/알림: `/api/admin/notifications/*` 브로드캐스트/타깃 발송
- [✅] 보상 대량지급: grant-bulk

### 8.11 데이터/스키마/마이그레이션
- [✅] Alembic 단일 head 유지, 변경 시 merge 전략
- [⏳] Crash/게임 관련 테이블 정식화 및 인덱스/제약 보강
- [⏳] 이벤트/미션/배틀패스 스키마 설계 및 마이그레이션 동반

### 8.12 Redis
- [✅] 크래시 세션 키/TTL, 사용자 streak/xp/pending_gems 등 캐시 키 설계 문서화
- [TBD] 캐시 무효화 전략(대규모 트래픽 시), 메모리/eviction 정책 점검

### 8.13 Kafka (선택)
- [⏳] health/produce/consume 스모크 및 토픽 구성 문서화
- [TBD] 재시도/DLQ/보상 트랜잭션 이벤트 스트리밍

### 8.14 관측/로깅/모니터링
- [✅] 구조화 로그(요청 id/사용자 id/경로/지연) 수집
- [TBD] 대시보드(체류/전환/에러율/p95), 알림 룰, 로그 보존 정책

### 8.15 프론트엔드
- [✅] `.env`/인터셉터(401→refresh→재시도), WebSocket 클라이언트(자동 재연결/heartbeat)
- [⏳] 주요 페이지(프로필/보상함/게임/상점) 계약/타입 최신화(openapi 기반)
- [✅] Jest/RTL: 리프레시/재시도 시나리오 테스트

### 8.16 OBS 스트리밍 컴포넌트(선택)
- [⏳] 혜택 강조 텍스트 오버레이(페이드/타이핑/슬라이드)
- [⏳] 랜딩 유도 배너/버튼(네온 글로우)
- [⏳] QR 오버레이(모바일 진입 가이드), 경량/성능 준수

## 9) 프로덕션 하드닝 체크리스트 (운영 준비/안정화)

설명: 상용 배포 전 필수 확인 항목. 가능한 한 자동화(CI/모니터링)로 검증.

### 9.1 SLO/목표 지표
- [ ] 가용성(예: 99.9%), p95/99 지연(핵심 API: auth/rewards/games), 오류율(5xx) 목표 정의 및 대시보드 반영
- [ ] 용량·비용 목표(동시 사용자, 초당 요청수, 메시지 처리량, 월간 비용 상한) 문서화

### 9.2 보안/비밀관리
- [ ] .env.production 분리/비공개, 시크릿은 Vault/Secrets(예: Docker secrets) 사용, 리포에 커밋 금지
- [ ] JWT_SECRET_KEY 로테이션 전략, 토큰 폐기/블랙리스트 경로 문서화
- [ ] TLS 종단(프록시/Nginx 등), HSTS, 보안 헤더(CSP/Frame-Options/Referrer-Policy)
- [ ] CORS 화이트리스트, 관리자 경로 보호, RBAC 최소 권한 원칙
- [ ] 의존성 스캔(SBOM), 취약점 알림/업데이트 프로세스

### 9.3 배포/인프라
- [ ] docker-compose.prod 리소스 제한(cpu/mem), restart 정책, 헬스체크 구성(backend /health, frontend)
- [ ] 무중단 전략(blue/green 또는 롤링), 데이터 마이그레이션 단계 분리
- [ ] 환경 분리(dev/stage/prod) 및 이미지 태깅/고정(pinned)

### 9.4 API 헬스/레디니스
- [ ] /health 단순 OK + 종속성 점검(Postgres/Redis/Kafka) 헬스 세부 확인
- [ ] 레디니스 지표(마이그레이션 완료, 캐시 준비, OpenAPI 로드) 반영

### 9.5 DB(PostgreSQL)
- [ ] 백업/복구 절차(PITR 또는 스냅샷), 정기 검증 리허설
- [ ] 연결 풀/pgBouncer(선택), 적절한 max_connections/statement_timeout
- [ ] 인덱스/쿼리 계획 검토, 장기 트랜잭션/데드락 모니터링
- [ ] Alembic: 단일 head 보장, backward-compatible 변경 원칙, 다운그레이드 경로/롤백 계획

### 9.6 Redis
- [ ] Persistence(AOF/RDB) 정책, eviction 정책, 메모리 경보 임계치
- [ ] 키 TTL/네임스페이스 표준, 캐시 무효화 전략(트래픽 급증 시)
- [ ] Sentinel/Cluster 고려(고가용성 필요 시)

### 9.7 Kafka(사용 시)
- [ ] 토픽 파티션/복제/retention 설정, 압축 옵션
- [ ] DLQ/재시도 정책, idempotent producer, consumer 그룹 오프셋 관리
- [ ] 라그(lag) 모니터링/알림

### 9.8 성능/부하 테스트
- [ ] 핵심 경로 부하 테스트(auth/rewards/games) 목표치 충족(p95, 에러율)
- [ ] 멱등키/트랜잭션 경합 시 일관성 보장, 스로틀/레이트리밋 정책 확인
- [ ] 캐시 히트율/DB QPS/락 대기 시간 대시보드화

### 9.9 관측성/로깅/추적
- [ ] 구조화 로그(요청 id/사용자 id/경로/지연), 샘플링 정책
- [ ] 대시보드(4대 시그널: 지연/에러/트래픽/포화) 및 알림 룰(SLO 위반, 5xx 급증, 큐 지연)
- [ ] 분산 추적(OpenTelemetry) 도입 검토(백/프론트, 큐 연계)

### 9.10 프론트엔드(Next.js)
- [ ] 코드 스플리팅/캐싱 헤더, 이미지 최적화(WebP), 성능 점수(LCP/CLS/TBT)
- [ ] 에러 바운더리/리포팅(Sentry 등), CSP 정합성
- [ ] SEO/OG/접근성(ARIA) 준수

### 9.11 개인정보/규정
- [ ] PII 인벤토리/데이터 흐름 문서, 최소 수집 원칙
- [ ] 저장 데이터 암호화/전송 암호화, 보관·파기 정책
- [ ] 동의/거부(트래킹/마케팅) 처리, 감사 로그

### 9.12 런북/DR/온콜
- [ ] 장애 대응 런북(탐지→완화→복구), 에스컬레이션 체계, 온콜 캘린더
- [ ] 롤백/기능 토글/DB 복구 리허설(정기)
- [ ] 정기 모의훈련(WS 장애, DB 장애, 캐시 장애, 메시지 지연)
